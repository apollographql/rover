query GetUserOrders($userId: ID!, $status: OrderStatus) {
  user(id: $userId) {
    id
    email
    orders(status: $status) {
      id
      createdAt
      total
      items {
        quantity
        product {
          ...ProductFields
        }
      }
    }
  }
}

"""
Baseline schema for the graph publish --check e2e tests.
Uses unique type names (GraphCheckBaselineResult) to avoid conflicts
with other schemas on the same variant.
"""
type Query {
  graphCheckBaseline: GraphCheckBaselineResult
}

type GraphCheckBaselineResult {
  id: ID!
  value: String!
}

directive @tag(name: String!) repeatable on FIELD_DEFINITION | OBJECT

type Cser @key(fields:"email") {
    email:ID! @tag(name: "test-from-csers")
    name: String
    totalProductsCreated: Int
}

type Panda @key(fields: "name"){
    name:ID!
    cser: Cser
}

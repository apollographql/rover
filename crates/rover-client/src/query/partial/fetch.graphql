query Query($implementingServicesGraphVariant: String!, $serviceId: ID!) {
  service(id: $serviceId) {
    implementingServices(graphVariant: $implementingServicesGraphVariant) {
      ... on FederatedImplementingServices {
        services {
          name
          activePartialSchema {
            sdl
          }
        }
      }
    }
  }
}

query ListTagsByGraphQuery($graphId: ID!, $first: Int, $after: String) {
    graphArtifactTags(graphID: $graphId, first: $first, after: $after) {
        totalCount
        pageInfo {
            hasNextPage
            endCursor
        }
        edges {
            node {
                tag
            }
        }
    }
}

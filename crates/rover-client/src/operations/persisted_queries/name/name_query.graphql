query PersistedQueryListNameQuery($graphId: ID!, $listId: ID!) {
  frontendUrlRoot
  graph(id: $graphId) {
    persistedQueryList(id: $listId) {
      name
    }
    variants {
      name
    }
  }
}
